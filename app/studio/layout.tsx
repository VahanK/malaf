// Internal marketing studio — never indexed, owner-facing only.
export const metadata = { robots: { index: false, follow: false } }

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children
}
